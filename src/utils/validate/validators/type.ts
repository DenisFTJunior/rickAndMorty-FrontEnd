export const isArray = (arr: any) => Array.isArray(arr)
export const isString = (str: any) => typeof str === 'string'
export const isBoolean = (boo: any) => typeof boo === 'boolean'
export const isFunction = (func: any) => typeof func === 'function'
